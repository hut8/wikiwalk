steps:
- name: bash
  args:
    - 'mkdir'
    - '-p'
    - './data/dumps/'
- name: gcr.io/cloud-builders/gsutil
  args:
    - 'cp'
    - 'gs://wikiwalk/dumps/enwiki-${_DUMP_DATE}-*.sql.gz'
    - './data/dumps/'
- name: bash
  args:
    - 'find'
    - './data/'
- name: 'gcr.io/cloud-builders/docker'
  args:
    - 'build'
    - '-t'
    - '${_SERVICE_IMAGE_TAGGED_LATEST}'
    - '-t'
    - '${_SERVICE_IMAGE_TAGGED}'
    - '-f'
    - 'Dockerfile.service'
    - '--build-arg'
    - 'DUMP_DATE=${_DUMP_DATE}'
    - '.'
- name: gcr.io/cloud-builders/docker
  args:
    - 'push'
    - '${_SERVICE_IMAGE_TAGGED_LATEST}'
- name: gcr.io/cloud-builders/docker
  args:
    - 'push'
    - '${_SERVICE_IMAGE_TAGGED}'
